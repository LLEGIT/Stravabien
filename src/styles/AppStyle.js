import { StyleSheet } from "react-native";

export const appStyle = new StyleSheet.create({
    tabNavigator: {
        profileIcon: { 
            width: 30,
            height: 30,
            borderRadius: 20 
        },

        profileIconClicked: {
            width: 30,
            height: 30,
            borderRadius: 20,
            borderColor: '#FC6100'
        }
    }
});